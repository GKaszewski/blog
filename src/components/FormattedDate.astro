---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="text-base" datetime={date.toISOString()}>
  {
    date.toLocaleDateString('en-us', {
      dateStyle: 'long',
    })
  }
</time>
